name: electra_evolution_input_profile_interactions
description: "Evolution phase 4: sample pairwise interactions across handler-specific input profile models."

generation:
  fork: electra
  preset: minimal
  handlers:
    - all
  per_handler_limit: 48
  mode: input_profile
  input_profile_order: 2
  guided: false
  changed_only: false
  unchanged_only: false
  invalid_only: false
  keep_existing: false

coverage:
  ontology: tests/generators/compliance_runners/state_transition/test_ontology.yaml
  html: true
  annotate: true
  json: true
