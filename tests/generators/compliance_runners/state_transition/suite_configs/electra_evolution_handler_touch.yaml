name: electra_evolution_handler_touch
description: "Evolution phase 1: one shallow generated vector per materialized Electra handler."

generation:
  fork: electra
  preset: minimal
  handlers:
    - all
  per_handler_limit: 1
  mode: handler_touch
  changed_only: false
  unchanged_only: false
  invalid_only: false
  keep_existing: false

coverage:
  ontology: tests/generators/compliance_runners/state_transition/test_ontology.yaml
  html: true
  annotate: true
  json: true
